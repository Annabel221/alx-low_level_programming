#include <stdio.h>
/**
 * main -A programming that prints a line using printf functions
 * Return: 0 (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n")
return (0);
}
